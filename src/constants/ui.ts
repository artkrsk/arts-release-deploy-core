/** Polling and debounce intervals in milliseconds */
export const INTERVALS = {
  POLL: 600,
  DEBOUNCE: 600
} as const

/** Size units for file size formatting */
export const SIZE_UNITS = ['B', 'KB', 'MB', 'GB'] as const

/** GitHub URL protocol prefix */
export const GITHUB_PROTOCOL = 'edd-release-deploy://' as const
